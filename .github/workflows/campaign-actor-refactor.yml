name: Campaign actor refactor

on:
  push:
    branches:
      - feat/campaign-actor-inventory
    paths:
      - scripts/apply-campaign-actor-refactor.py
      - .github/workflows/campaign-actor-refactor.yml
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  refactor:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/campaign-actor-inventory
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: python3 scripts/apply-campaign-actor-refactor.py
      - run: npm ci
      - run: npx prisma generate
      - run: npx prisma validate
      - run: npm run test:unit
      - run: npm run build:web
      - name: Commit validated refactor
        run: |
          rm -f scripts/apply-campaign-actor-refactor.py
          rm -f .github/workflows/campaign-actor-refactor.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "refactor: replace Character with CampaignActor" || exit 0
          git push origin HEAD:feat/campaign-actor-inventory
