# VTT — Nota de migração documental

Status: **CURRENT**

A documentação deste domínio foi consolidada em `docs/features/vtt/` durante QH-GOV-009.

## Fontes atuais

- contrato geral: `docs/features/vtt/spec.md`;
- arquitetura de Token: `docs/features/vtt/token-architecture.md`;
- acesso agnóstico à ficha: `docs/features/vtt/token-sheet-access.md`;
- arquitetura superior: `docs/ARCHITECTURE.md`;
- decisões de Member/Actor/Token: ADR-0003 e ADR-0004.

## Referência histórica embutida

O `spec.md` foi preservado porque contém um contrato operacional extenso ainda útil. A referência antiga à arquitetura de Token que permanece no texto deve ser lida como `docs/features/vtt/token-architecture.md`.

A referência histórica não autoriza recriar a estrutura documental removida. Em caso de divergência, Constitution, ADRs e `docs/ARCHITECTURE.md` prevalecem.
